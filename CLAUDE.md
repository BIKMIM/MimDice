# MimDice 작업 규칙

와우 애드온 프로젝트입니다. 사용자와는 **한국어**로 대화합니다.

## 코드를 고치기 전에

**[DEVGUIDE.md](DEVGUIDE.md)를 먼저 읽으세요.**
12.1 비밀 값 대응 과정에서 겪은 실수와 실측 데이터가 정리되어 있습니다.
특히 **오라(버프/디버프) 감지, 사운드 알림, 설정창 레이아웃**을 건드릴 때는 필수입니다.

## 잊으면 안 되는 것 (자세한 내용은 DEVGUIDE.md)

1. **블러드 감지에 `C_UnitAuras.AddAuraSound` / `RemoveAuraSound` 등록 계층을 다시 넣지 않습니다.** `AddAuraSound` 차단은 `pcall`로 막을 수 없고, 실측한 던전에서는 Map 제한이 활성 상태였습니다.
2. **비밀 값 검사 순서는 `issecretvalue` → `type` → 비교** 순입니다. 비밀 문자열도 `type()`은 `"string"`을 반환하므로 형식 검사만으로는 못 거릅니다.
3. **오라 감지는 `GetPlayerAuraBySpellID` 조회 방식**을 씁니다. 블러드 후유증은 전부 NeverSecret으로 확인됐습니다.
4. **실제로 적용된 알림의 누락 방지가 우선입니다.** 불가피한 중복은 허용하지만, 시전만 보고 울리지는 않습니다. 죽음·사거리 밖 등으로 후유증을 받지 못했다면 알리지 않는 것이 정상입니다.
5. **팝업 금지, 쉬운 한국어, 기존 사용자 설정 보존.**

## 배포

- 버전은 **3곳 모두** 수정: `MimDice.toc`, `MimDice.lua`, `MimDice.xml`
- 커밋 메시지는 한국어
- **푸시는 사용자가 명시적으로 요청할 때만**
- 배포 전 게임 테스트 항목은 DEVGUIDE.md 7장 참고
